import styled from "styled-components";
import tw from "twin.macro";

export const StyledSubmitButtonElement = styled.button`

${tw `text-white text-sm px-2 bg-blue py-3 font-semibold border-0 rounded text-center w-full`}`

export const StyledSubmitButtonContainer = styled.div`${tw`flex justify-items-center w-full text-center`}`

export const StyledSubmitButtonLoaderElement = styled.div`${tw`text-white text-sm px-2 bg-blue py-3 font-semibold border-0 rounded text-center w-full flex justify-items-center items-center`}`

export const StyledLoaderContainer = styled.div`${tw `w-full flex justify-items-center`}`