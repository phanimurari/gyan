import styled from "styled-components";
import tw from "twin.macro";

export const StyledButtonElement = styled.button`

${tw `text-white text-sm bg-blue-600 px-2 py-3 font-semibold border-0 rounded`}`